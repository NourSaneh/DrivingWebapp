import React from 'react';

const FeatureCard = ({ icon, iconColor, title, description, buttonText, buttonColor, hoverColor, onClick }) => {
    return (
<div> test</div>
    );
};

export default FeatureCard;